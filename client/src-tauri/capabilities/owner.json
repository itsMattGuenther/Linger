{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "owner",
  "description": "The app's own commands for the list window, `main` in both clients (docs/design/architecture.md, the owner). Only it holds the sign-ins in the keyring, the server connections, voice, notifications and other windows' opening; build.rs declares every command, so no other window can call these unless its own capability file grants one. The old client is a single `main` window and uses all of them.",
  "windows": [
    "main"
  ],
  "permissions": [
    "allow-sessions-load",
    "allow-session-save",
    "allow-session-forget",
    "allow-gateway-connect",
    "allow-gateway-disconnect",
    "allow-gateway-token",
    "allow-gateway-send",
    "allow-voice-join",
    "allow-voice-leave",
    "allow-voice-frame",
    "allow-voice-controls",
    "allow-voice-volume",
    "allow-voice-devices",
    "allow-show-notification",
    "allow-app-version",
    "allow-update-check",
    "allow-update-install",
    "allow-graphics-started",
    "allow-next-open-chat",
    "allow-next-open-conversation",
    "allow-next-open-settings",
    "allow-next-open-tool",
    "allow-next-close-to-tray",
    "allow-next-tray-voice"
  ]
}
