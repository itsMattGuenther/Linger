import "./Spinner.css";

/** A small busy indicator, the size of a `sm` icon box. Decoration only. */
export function Spinner() {
  return <span className="k-spinner" data-kit="Spinner" aria-hidden="true" />;
}
